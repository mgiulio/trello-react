var
   //React = require('../lib/react/react'),
   About = React.createClass({

      render: function() {
         return (
            <h1>About</h1>
         );
      }

   })
;

module.exports = About;
